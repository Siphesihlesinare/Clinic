<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="http://prismlibrary.com"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="WomensClinicApp.Views.Signup">
    <StackLayout VerticalOptions="CenterAndExpand">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Label Text="Sign Up"/>
        <Entry Text="{Binding UsersDetails.Name}" Placeholder="Name" />
        <Entry Text="{Binding UsersDetails.Surname}" Placeholder="Surname" />
        <Entry Text="{Binding UsersDetails.IDN}" Placeholder="ID Number" />
        <Entry Text="{Binding UsersDetails.Adress}" Placeholder="Address" />
        <Entry Text="{Binding UsersDetails.Phone}" Placeholder="Cell Phone Number" />
        <Entry Text="{Binding UsersDetails.Password}" Placeholder="Password" />
        <Entry Text="{Binding UsersDetails.Confirm}" Placeholder="Confirm Password" />
        <Button Text="Submit"  Command="{Binding SubmitCommand}"/>
           
</Grid>
    </StackLayout>
</ContentPage>